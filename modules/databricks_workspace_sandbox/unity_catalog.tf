# Catalogs will be created per participant/company when they onboard.
# e.g. databricks_catalog "company_a" { ... }
# Schemas (raw_data, staging, curated, shared) will live inside each company catalog.
# For now, use the default 'main' catalog for testing.
